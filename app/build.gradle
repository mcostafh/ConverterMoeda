apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'convertermoeda'
            keyPassword 'Cel88964418'
            storeFile file('C:/Users/Manuel/AndroidstudioProjects/keystore_apps3.jks')
            storePassword 'Cel88964418'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "br.com.tdm.convertermoeda"
        minSdkVersion 17
        targetSdkVersion 21
        versionName '2.0'
        versionCode 2
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:21.0.3'
}
